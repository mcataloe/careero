# Frontend

The frontend is a local React + Vite + TypeScript application for Careero Layer 1.

Run from this directory:

```powershell
npm install
npm run dev
```

Build validation:

```powershell
npm run build
```

If PowerShell blocks `npm`, use `npm.cmd`.
